    //declarando os valores do vetor
    vetor = [85,1,67,32,99,41,3,19,56,10,87];

    //função que ordena vetor
    function sortFunction(x, y){
        return (x-y);
    }

    //exibe informação
    console.log (vetor.sort(sortFunction));